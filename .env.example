# ── Database ───────────────────────────────────────────────────
# Local dev (SQLite):
DATABASE_PROVIDER="sqlite"
DATABASE_URL="file:./mycelium.db"

# Production (Postgres — Neon, Supabase, Vercel Postgres, Railway):
# DATABASE_PROVIDER="postgresql"
# DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"

# ── AI ─────────────────────────────────────────────────────────
# Required: Claude API for capability matching + task execution
ANTHROPIC_API_KEY=""

# Optional: Tavily for real web search (web_search capability)
TAVILY_API_KEY=""
